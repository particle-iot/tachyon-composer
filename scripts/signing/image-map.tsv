# Tachyon boot/firmware blob -> sectoolsv2 image-id map.
#
# Drives scripts/signing/sign.sh, which re-signs each blob with the selectable key:
#   sectools secure-image <file> --image-id <id> --security-profile kodiak_security_profile.xml \
#            --outfile <out> --sign --signing-mode <TEST|LOCAL>
#
# Columns (tab-separated):
#   filename     the blob filename as it appears in the BP QCM6490_bootbinaries set
#   image_id     the sectoolsv2 image-id (kodiak_security_profile.xml) to (re)sign it with
#   sign         yes|no|metabuild
#                  yes       -> per-blob secure-image --sign
#                  metabuild -> regenerated via metabuild-secure-image (vouches for other hashes)
#                  no        -> passed through unchanged
#   note         provenance / caveats
#
# image_ids below were derived AUTHORITATIVELY: each released (already-signed) blob only passes
# `secure-image --image-id <id> ... --validate` against its correct id. Verified against the
# QCM6490 bp-fw 2.0.0 bootbinaries. Filesystem/data images are not Qualcomm-signed.
#
# filename		image_id		sign		note
xbl.elf			XBL			yes
xbl_config.elf		XBL-CONFIG		yes
xbl_config_kvm.elf	XBL-CONFIG		yes
xbl_config_gunyah.elf	XBL-CONFIG		yes
uefi.elf		UEFI			yes
uefi_sec.mbn		-			no		FV component folded into uefi.elf; not independently signable as UEFIFV (CONFIRM)
imagefv.elf		-			no		FV component folded into uefi.elf; not independently signable as UEFIFV (CONFIRM)
tz.mbn			TZ			yes
hypvm.mbn		QHEE			yes
aop.mbn			AOP			yes
devcfg.mbn		TZ-DEVCFG		yes
cpucp.elf		CPUCP			yes
shrm.elf		SHRM			yes
qupv3fw.elf		QUPV3			yes
XblRamdump.elf		XBL-RAM-DUMP		yes
prog_firehose_ddr.elf	DEVICE-PROGRAMMER	yes
prog_firehose_lite.elf	DEVICE-PROGRAMMER	yes
multi_image.mbn		-			metabuild	regenerate: vouches for the re-signed images' hashes
tools.fv		-			no		did not validate as TOOLS-FV; not re-signed (CONFIRM)
partition_emmc.xml	-			no		partition table, not signed
partition_ufs.xml	-			no		partition table, not signed
logfs_ufs_8mb.bin	-			no		log fs, not signed
cdt.bin			-			no		chip data table, not signed
efi.img			-			no		FAT ESP, not Qualcomm-signed
nonhlos.img		-			no		FAT firmware image (pre-built in bp-fw artifact)
dtb.img			-			no		FAT dtb image, not Qualcomm-signed
