[Unit]
RequiresMountsFor=/vendor /persist

[Service]
# Use the existing EFS partitions by label, with normal modem writes enabled.
# QLI's reference unit adds -r, which prevents persisting modem state.
ExecStart=
ExecStart=/usr/bin/rmtfs -P -s
