[Unit]
# QLI symlinks /var/log to /var/volatile/log. Wait for the actual backing
# mount before opening the journal, or that mount hides the open log files.
RequiresMountsFor=/var/volatile/log/journal
